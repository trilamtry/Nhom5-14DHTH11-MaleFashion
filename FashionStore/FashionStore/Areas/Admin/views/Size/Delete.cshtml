@model FashionStore.Models.Size

@{
    ViewBag.Title = "Xóa Kích thước";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="page-header">
    <h1 class="page-title text-danger">Xác nhận xóa Kích thước</h1>
    <p class="page-subtitle">Hành động này sẽ gỡ bỏ kích cỡ này khỏi hệ thống vĩnh viễn.</p>
</div>

<div class="d-flex justify-content-center">
    <div class="content-card col-12 col-md-6 border-danger-subtle shadow-lg">
        <div class="card-header" style="background: rgba(239, 68, 68, 0.05);">
            <div class="card-title text-danger">
                <i class="bi bi-exclamation-octagon-fill"></i> Cảnh báo hệ thống
            </div>
        </div>

        <div class="card-body">
            @Html.ValidationSummary(true, "", new { @class = "alert alert-danger p-2 small mb-3" })

            <div class="alert-item mb-4" style="background: rgba(239, 68, 68, 0.1); border-color: var(--danger);">
                <div class="alert-info text-center w-100">
                    <div class="variant-info mb-1">Bạn đang thực hiện xóa kích cỡ:</div>
                    <div class="display-3 fw-bold text-danger">@Model.SizeCode</div>
                </div>
            </div>

            <div class="p-3 rounded mb-4 text-center" style="background: rgba(255,255,255,0.02); border: 1px dashed var(--border-color);">
                <p class="text-secondary mb-0">
                    <i class="bi bi-info-circle me-1"></i>
                    Lưu ý: Nếu Size này đang được liên kết với sản phẩm trong kho, hệ thống sẽ từ chối xóa để bảo vệ dữ liệu.
                </p>
            </div>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.SizeId)

                <div class="d-grid gap-2 mt-4">
                    <button type="submit" class="btn btn-primary py-3" style="background: var(--danger); border: none; font-weight: 600;">
                        <i class="bi bi-trash3-fill me-2"></i> Xác nhận xóa ngay
                    </button>
                    <a href="@Url.Action("Index")" class="btn btn-secondary text-center py-2">
                        Hủy bỏ và quay lại
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<style>
    /* Hiệu ứng rung nhẹ khi trang load để thu hút sự chú ý vào khung cảnh báo */
    .border-danger-subtle {
        border: 1px solid rgba(239, 68, 68, 0.2) !important;
        animation: shake 0.5s ease-in-out;
    }

    @@keyframes shake {
        0%, 100% {
            transform: translateX(0);
        }

        20%, 60% {
            transform: translateX(-5px);
        }

        40%, 80% {
            transform: translateX(5px);
        }
    }
</style>